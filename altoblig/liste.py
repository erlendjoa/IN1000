
class Liste:
    def __init__(self):
        self.liste = f""
    def split(self, tegn):
        for tegn in self.liste:
            tegn.ge
    def append(self, element):
        self.liste = f" " + f", {element}"